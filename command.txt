// For the first time - git setup

1. git config --global user.name "ncertmohan"
2. git config --global user.email "ncertmohan@gmail.com"

// To convert a normal folder into a git repository. we need to execute
1. git init
2. git add .
3. git commit -m "message"